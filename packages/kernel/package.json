{
  "name": "@cardstock/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Geometry compute. OpenCascade WASM in a Web Worker behind an RPC boundary.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@cardstock/types": "*",
    "opencascade.js": "^1.1.1",
    "replicad-opencascadejs": "^1.1.0"
  }
}
