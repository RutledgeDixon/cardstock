[package]
name = "cardstock"
version = "0.7.0"
description = "CARDstock — parametric solid modelling, simplified."
authors = ["Rutledge Dixon"]
license = "UNLICENSED"
edition = "2021"
rust-version = "1.77"

[lib]
name = "cardstock_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
